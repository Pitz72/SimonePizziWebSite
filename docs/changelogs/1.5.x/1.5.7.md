# Changelog v1.5.7 - Security Fix & Ordinamento Articoli Programmati

**Data Rilascio:** 23 Marzo 2026

Questa release risolve due bug indipendenti emersi dall'analisi di sicurezza della codebase: una vulnerabilita di accesso non autenticato alle bozze tramite API e un difetto logico nell'ordinamento degli articoli programmati che ne alterava la posizione nel feed al momento della pubblicazione.

---

## 1. Fix Sicurezza: Accesso Non Autenticato a Bozze via API (?id=X)

### Problema

L'endpoint GET  non richiedeva autenticazione. Chiunque conoscesse (o indovinasse per enumerazione sequenziale) l'ID numerico di un articolo poteva chiamare direttamente l'API e ricevere il payload JSON completo () dell'articolo, **incluse bozze e articoli programmati per il futuro**, senza essere loggato nel pannello admin.

A differenza dell'endpoint gemello  (che implementava correttamente il controllo di visibilita pubblica con fallback a 404 per contenuti non pubblicati), il percorso  — usato esclusivamente dal React Editor del pannello admin per caricare un articolo in modifica — era privo di qualsiasi guardia di autenticazione.

**Scenario di attacco:**


### Causa Radice

Il branch  nel blocco GET di  era stato progettato come endpoint interno del pannello admin, assumendo implicitamente che solo l'admin vi accedesse. Mancava la chiamata esplicita ad  che invece tutti gli altri metodi write (POST, PUT, DELETE, PATCH) implementavano correttamente.

### Risoluzione

Aggiunta la chiamata  come prima istruzione del branch , immediatamente dopo l'ingresso nel blocco condizionale. Se la sessione admin non e' presente, il server risponde con  e termina l'esecuzione prima di interrogare il database.

**File modificato:**  (riga 71)



---

## 2. Fix Logico: Ordinamento Articoli Programmati nel Feed

### Problema

Gli articoli pubblicati tramite lo scheduler (con  impostato a una data futura) comparivano nel feed pubblico — Homepage e pagine categoria — ordinati per **data di creazione** (), non per **data di pubblicazione effettiva** ().

**Comportamento errato (esempio pratico):**

| Articolo | Creato il | Pubblicato il | Posizione attesa | Posizione reale |
|----------|-----------|---------------|-----------------|-----------------|
| Articolo A | 01 Mar | 01 Mar | 1° (piu recente) | 2° |
| Articolo B (schedulato) | 15 Feb | 22 Mar | 1° (piu recente) | 3° |
| Articolo C | 10 Feb | 10 Feb | 3° | 1° (errato!) |

L'articolo B veniva creato il 15 Febbraio con pubblicazione schedulata al 22 Marzo. Quando si sbloccava, risultava ordinato dalla sua data di *creazione* (15 Feb), apparendo sepolto nel feed sotto articoli creati e pubblicati successivamente.

Il feed RSS (file ) non aveva questo problema in quanto gia usava . La divergenza tra i due ordinamenti creava un'incongruenza tra il feed del sito e il feed RSS.

### Causa Radice

La query principale di  per il listing degli articoli utilizzava:

invece di considerare  come criterio di ordinamento primario.

### Risoluzione

Sostituita la clausola  con un'espressione  che usa  quando disponibile (non NULL e non stringa vuota, per compatibilita retroattiva con articoli legacy) e  come fallback per articoli privi di data di pubblicazione esplicita:



Questo garantisce:
- Gli articoli schedulati appaiono in cima al feed **al momento del loro sblocco**, nella posizione cronologica corretta.
- Gli articoli legacy (importati con  NULL o vuoto) continuano a ordinarsi per data di creazione, senza regressioni.
- Il comportamento del feed pubblico e' ora allineato con quello del feed RSS.

**File modificato:**  (riga 107)

---

## Note Tecniche

- Entrambe le fix riguardano esclusivamente .
- Nessuna modifica al database, nessuna migrazione necessaria.
- Nessuna modifica al frontend React.
- Il fix di sicurezza e' retroattivo: l'endpoint  era vulnerabile dalla v1.3.1 (introduzione dell'Admin Editor).
- Il fix dell'ordinamento e' retroattivo: il bug era presente dalla v1.5.0 (introduzione dello scheduler).
