# Simone Pizzi Portfolio - v1.8.5

**Data Rilascio**: 22 Aprile 2026
**Autore**: Simone Pizzi & Antigravity

## Oggetto della Sessione

Implementazione della paginazione e della ricerca avanzata nel backend (Gestione Articoli).

## Changelog Dettagliato

### 🛡️ Admin & CMS

- **Paginazione Backend-Driven**: Implementato il supporto alla paginazione nella lista articoli dell'area admin. Gli articoli vengono caricati a blocchi di 10 (configurabile), riducendo il carico sul browser e migliorando le performance con cataloghi estesi.
- **Ricerca Avanzata**: Potenziato il motore di ricerca interno del backend. Ora è possibile filtrare gli articoli per:
  - **Query Testuale**: Ricerca libera su titolo, riassunto e contenuto.
  - **Categoria**: Filtro drop-down per le categorie esistenti.
  - **Tag**: Filtro drop-down per i tag dinamici registrati nel sistema.
  - **Range Temporale**: Filtri "Dal" e "Al" basati sulla data di pubblicazione.
- **UI/UX Optimization**: Aggiornata l'interfaccia della lista articoli con una barra di ricerca multifunzione e controlli di paginazione moderni con feedback visivo dello stato di caricamento.

### 🏗️ Backend (API)

- **articles.php**: Aggiornato l'endpoint per supportare i parametri `q`, `tag`, `start_date` e `end_date`.
- **SQL Optimization**: La query di conteggio totale (`COUNT(*)`) ora rispetta tutti i filtri applicati, garantendo che la paginazione sia sempre coerente con i risultati filtrati.
- **API Client**: Esteso il metodo `getArticles` in `api.ts` per supportare il nuovo oggetto parametri.

### 🛠️ Hotfix & Build Stability

- **Refactoring Signature API**: Identificata e risolta un'incoerenza TypeScript causata dal passaggio da argomenti posizionali a oggetto nel metodo `api.getArticles`.
- **Allineamento Componenti**: Aggiornati i seguenti file per garantire la compatibilità con la nuova firma del metodo:
  - `InternalLinkSelector.tsx` (Selettore link interni nell'editor).
  - `NewsletterAdmin.tsx` (Selezione articoli per invio newsletter).
  - `useFetchArticles.ts` (Hook di caricamento nel frontend pubblico).
- **Cleanup**: Rimossi import inutilizzati segnalati dal compilatore in `ArticlesList.tsx`.
- **Build Verification**: Eseguito ciclo di build completo con successo (Exit code 0), inclusa l'esecuzione degli script di pulizia `clean-dist.js`.

---
*Documento generato automaticamente post-rilascio.*
