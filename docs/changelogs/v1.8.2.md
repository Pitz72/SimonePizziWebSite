# Simone Pizzi Portfolio - v1.8.2

**Data Rilascio**: 22 Aprile 2026
**Autore**: Simone Pizzi & Antigravity

## Oggetto della Sessione

Risoluzione del bug critico che impediva l'applicazione e la visualizzazione dei colori nel testo degli articoli dopo la migrazione a Tiptap.

## Changelog Dettagliato

### 🛠️ Bugfix Editor e SEO

- **Color Application Fix**: 
  - Aggiornata la configurazione di **DOMPurify** in `SingleArticle.tsx` per autorizzare l'attributo `style`. Questo permette di preservare i tag `<span style="color: ...">` generati da Tiptap, risolvendo la scomparsa dei colori nel frontend.
  - Rimossa la classe CSS forzata `text-zinc-300` dall'area di editing in `RichTextEditor.tsx`. Questo garantisce che i colori personalizzati abbiano la precedenza sulle classi Tailwind Typography.
  - Ottimizzata la gestione del focus nella palette colori aggiungendo `onMouseDown={(e) => e.preventDefault()}` sui singoli campioni, evitando la perdita della selezione del testo durante la scelta del colore.

### 📝 Manutenzione

- **Versione 1.8.2**: Aggiornamento di `package.json` e `MASTER_PLAN.md`.
- **Build**: Generata nuova build di produzione verificata e sicura.

---
*Documento generato automaticamente post-bugfix.*
