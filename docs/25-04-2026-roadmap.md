# Roadmap Evolutiva — Simone Pizzi Website
**Data:** 25 Aprile 2026 (aggiornata 26 Aprile 2026)
**Versione corrente:** 1.14.0

---

## ✅ Completato

- **Automazione Prerendering** (v1.12.0) — Dynamic Rendering on-demand elimina i file statici
- **Bug Robots.txt** (v1.12.0) — `robots.php` servito sempre incondizionatamente da `.htaccess`
- **Sitemap Dinamica** (v1.12.0) — `sitemap.php` real-time con `<lastmod>` e sottocategorie
- **Conformità GDPR** (v1.12.1) — `privacy.php`, `cookie-policy.php`, banner JS, link Footer e form contatti
- **Reazioni agli Articoli** (v1.13.0) — 5 tipi con SVG cross-platform, toggle, optimistic update, rate limiting, GDPR-compliant
- **SEO Score & Analytics Articolo** (v1.14.0) — Pannello SEO real-time e grafici visualizzazioni per-articolo nell'editor admin
- **Statistiche Reazioni in Dashboard** (v1.14.0) — Riepilogo globale engagement (totali e per articolo) nell'area admin

---

## 🏗 Infrastruttura & Architettura

1. **Framework Backend** ⏸️ *SOSPESO* — Valutare migrazione API verso Laravel o Slim per routing strutturato, middleware e test. Costo sproporzionato per traffico attuale; da rivalutare se il progetto evolve verso un CMS multi-utente o API pubbliche.
2. **PWA & Offline Support** — Trasformare il sito in una Progressive Web App per consultazione offline (modalità fiera/presentazione).
3. **WebSocket** — Notifiche push istantanee lato admin per nuovi messaggi e iscrizioni newsletter.

---

## 📈 SEO & Analytics

4. **Integrazione Google Search Console** — Collegare la dashboard admin alle API GSC per monitorare query e posizionamento senza uscire dal CMS.
5. ~~**SEO Score per Articolo**~~ ✅ **COMPLETATO in v1.14.0** — Indicatore visivo real-time: lunghezza title/desc, keyword, immagini.
6. ~~**Grafico visualizzazioni per articolo**~~ ✅ **COMPLETATO in v1.14.0** — Grafico storico 30gg integrato nell'editor (Line Chart).
6-bis. ~~**Statistiche Reazioni in Dashboard**~~ ✅ **COMPLETATO in v1.14.0** — Card totali, grafico distribuzione per tipo e classifica articoli più votati.

---

## 🔐 Sicurezza & Gestione

7. **Autenticazione a Due Fattori (2FA)** — Aggiungere TOTP (es. Google Authenticator) per l'accesso admin.
8. **Multi-utente con Ruoli** — Ruoli Autore (bozze), Editor (pubblica), Admin (tutto). Include log accessi con IP e timestamp.
9. **Test Automatizzati** — Unit test (PHPUnit/Jest) e test d'integrazione (Cypress/Playwright) per prevenire regressioni.
10. **Backup Manuale dal CMS** — Pulsante "Esporta backup DB" in JSON/SQL dall'area admin.

---

## 🎨 UX Visitatore

11. **Tempo di lettura stimato** 🔴 — Badge "5 min di lettura" calcolato sul conteggio parole del contenuto (media 200 parole/minuto).
12. **Articoli Correlati** 🔴 — 3 card in fondo all'articolo, stessa categoria o tag in comune, escludendo l'articolo corrente.
13. **Pagina Tag** (`/tag/slug`) 🔴 — Ogni tag cliccabile porta a una pagina archivio con rotta React, loader, meta OG e supporto SEO Engine.
14. ~~**Reazioni agli Articoli**~~ ✅ **COMPLETATO in v1.13.0** — 5 reazioni con SVG cross-platform (thumb/heart/fire/think/game), toggle con optimistic update, rate limiting PHP, hash SHA256 anonimo GDPR-compliant.
15. **Commenti** 🟠 — Sistema commenti moderato. Opzione consigliata: Giscus (GitHub Discussions, zero tracking, gratis). Alternativa: sistema custom PHP leggero. *Prerequisito: sistema reazioni già rodato.*
16. **Table of Contents** 🟠 — Sidebar (desktop) o sezione espandibile (mobile) con heading `<h2>`/`<h3>` dell'articolo, scroll animato al click.
17. **Archivio Cronologico** (`/archivio`) 🟡 — Articoli raggruppati per anno e mese, stile "journal".
18. **Modalità Lettura "Focus"** 🟡 — Toggle che nasconde Header/Footer, aumenta font, sfondo neutro.
19. **Localizzazione (i18n)** 🟡 — Supporto multi-lingua, priorità versione inglese.
20. **Light Mode** 🟡 — Tema chiaro alternativo per accessibilità.
21. **Ottimizzazione Media Dinamica** 🟡 — Servizio immagini con `srcset` dinamico per dimensioni schermo.

---

## 🔧 UX Amministratore

22. **Anteprima Articolo** 🔴 — Pulsante "Anteprima" che apre il frontend con `?preview=1` per visualizzare la bozza prima della pubblicazione.
23. **Duplica Articolo** 🔴 — Pulsante "Duplica" nella lista articoli che crea una bozza identica.
24. **Slug Editor Manuale** 🟠 — Campo slug modificabile nell'editor (ora auto-generato dal titolo, non editabile da UI).
25. **Bulk Actions** 🟠 — Selezione multipla nella lista articoli: pubblica tutti, metti in bozza, elimina.
26. **Crop/Resize Immagine di Copertina** 🟠 — Crop diretto nell'admin (Sharp è già installato come dipendenza).
27. **Revisioni Articolo** 🟡 — Storico ultime N versioni per articolo con ripristino.
28. **SEO Score nell'Editor** 🟠 — *Vedi punto 5 SEO & Analytics.*

---

## ⚖️ Newsletter

29. **Compositore Visuale Newsletter** — Editor visuale per comporre email selezionando articoli esistenti e generando HTML email responsive.
30. **Statistiche Email** — Tasso apertura e click per ogni invio (pixel tracking + redirect link).

---

## 🗳️ Piano Dettagliato — Reazioni agli Articoli (Punto 14)

### Analisi di Fattibilità (6 righe)

Il sistema di reazioni è fattibile in una singola sessione di lavoro: richiede una nuova colonna JSON `reactions` nella tabella `articles` (o una tabella separata `article_reactions` per evitare lock), un endpoint PHP `POST /api/reactions.php` senza autenticazione, e un componente React `ReactionBar.tsx` client-side. Per prevenire voti multipli senza richiedere un account si usa una chiave composita `ip + article_id` su una tabella dedicata oppure un cookie/localStorage lato client (approccio più leggero e privacy-friendly). Le emoji candidate sono 5: 👍 Utile · ❤️ Bello · 🔥 Interessante · 🤔 Fa pensare · 🎮 Game-related (coerente col brand). Il componente si aggancia al SEO Engine senza modifiche: non richiede SSR perché le reazioni sono interazione pura client-side, visibili solo dopo l'idratazione React.

### Architettura Tecnica

**Database — nuova tabella:**
```sql
CREATE TABLE article_reactions (
    id          INT AUTO_INCREMENT PRIMARY KEY,
    article_id  INT NOT NULL,
    emoji       ENUM('👍','❤️','🔥','🤔','🎮') NOT NULL,
    voter_hash  VARCHAR(64) NOT NULL,   -- SHA256(ip + user_agent) anonimo
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY unique_vote (article_id, voter_hash, emoji),
    INDEX idx_article (article_id)
);
```

**Endpoint PHP** — `public/api/reactions.php`:
- `GET ?article_id=X` → restituisce conteggi per ogni emoji
- `POST {article_id, emoji}` → aggiunge/rimuove reazione (toggle), protetto da rate limiting IP

**Componente React** — `src/components/ReactionBar.tsx`:
- Mostra 5 bottoni emoji con contatore
- Stato locale con `useState` per risposta immediata (optimistic update)
- Chiamate a `api.getReactions(id)` nel loader di `SingleArticle` (già presente)
- Cookie `reacted_${articleId}` per persistenza locale senza account
- Animazione micro `scale(1.3)` al click con Framer Motion

**Posizione nel layout:**
```
[Contenuto articolo]
────────────────────
[ReactionBar]         ← tra contenuto e CTA
[CTA PayPal/Condividi]
[NewsletterSignup]
```

### Punti di Attenzione
- **Privacy:** `voter_hash` è SHA256 anonimo, non identifica l'utente, conforme GDPR
- **Spam:** Rate limiting a livello PHP (max 10 reazioni/minuto per IP) + `UNIQUE KEY` su DB previene duplicati
- **Mobile:** Bottoni min 44px touch target, layout flex-wrap per schermi piccoli

---

## 🚀 Legenda Priorità

| Simbolo | Significato |
|:---|:---|
| 🔴 Alta priorità | Alto impatto, bassa/media complessità |
| 🟠 Media priorità | Migliora significativamente la piattaforma |
| 🟡 Bassa priorità | Nice-to-have o alta complessità |
| ✅ Completato | In produzione |
| ⏸️ Sospeso | Da rivalutare in futuro |

---

*Roadmap aggiornata il 26 Aprile 2026 — Piano reazioni v1.13.0*
